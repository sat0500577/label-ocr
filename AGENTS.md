# 이 저장소에서 작업할 때

상품 라벨 사진에서 "제품명/색상"을 뽑는 개인용 웹앱이다. 서버가 없고, 촬영·인식·추출이
전부 브라우저 안에서 끝난다. GitHub Pages로 배포된다.

## 먼저 읽을 것

- **[README.md](README.md)** — 동작 원리, 추출 규칙, 속도 특성이 모두 여기 있다.
  코드를 고치기 전에 반드시 읽어라. "실측 확인"이라고 적힌 항목은 모두 직접 재현해서
  확인한 것이고, 되돌리면 같은 버그가 재발한다.
- **[.claude/skills/label-ocr/SKILL.md](.claude/skills/label-ocr/SKILL.md)** — 인식이
  틀리게 나올 때의 디버깅 절차와 함정 목록. Claude Code는 자동으로 읽지만, 다른 도구를
  쓴다면 직접 열어 봐야 한다.

## 지켜야 할 것

- **화면 캡처로 짐작해서 고치지 마라.** 캡처는 화면 크기로 줄어든 이미지라 실제 사진보다
  흐리다. 내 환경에서는 통과하는데 폰에서는 실패하는 일이 반복된다. 원본 사진을 받아라.
- **한 곳을 고치면 다른 곳이 깨진다.** README 끝의 검증 조건(원본·흐림·어두움·기울어짐·
  멀리서·회전 등)을 라벨 두 종류로 **전부** 다시 돌려라.
- **`index.html`을 고쳤으면 `sw.js`의 `CACHE` 이름을 올려라.** 안 올리면 폰에 예전 버전이
  캐시된 채로 남는다.
- 산출물·주석·문서는 한국어로 쓴다.
- 특정 사진 하나를 맞히려고 규칙을 더 복잡하게 만들지 마라. 다른 사진이 깨진다.

## 여러 도구를 같이 쓸 때

Claude Code와 ChatGPT를 같은 저장소에 함께 써도 된다. 다만 같은 파일을 동시에 고치면
충돌이 나므로, 작업 전에 `git pull`부터 하고 한 번에 한 쪽만 손대는 편이 안전하다.
